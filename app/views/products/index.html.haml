%h1 Products List
%ul
  - @category.each do |category|
    %h2= category.title
    - category.products.each do |product|
      %li
        = product.title
        = product.content
        = product.price